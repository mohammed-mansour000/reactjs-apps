import React from 'react'
import spinner from '../../images/spinner.gif';

const Spinner = () => {
    return (
        <img src={spinner} style={{ width: '200', margin: 'auto', display: 'block' }}/>
    )
}

export default Spinner
